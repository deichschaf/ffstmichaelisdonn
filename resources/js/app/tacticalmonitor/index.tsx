import React from 'react';
import { AppTacticalMonitorProps } from '../../props/props';

const AppTacticalMonitor: React.FC<React.PropsWithChildren<AppTacticalMonitorProps>> = (
  props: AppTacticalMonitorProps
): JSX.Element => {
  return <></>;
};
export default AppTacticalMonitor;
