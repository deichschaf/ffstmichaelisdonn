import React from 'react';
import { AppOverviewProps } from '../props/props';

const AppOverview: React.FC<React.PropsWithChildren<AppOverviewProps>> = (
  props: AppOverviewProps
): JSX.Element => {
  return <></>;
};
export default AppOverview;
