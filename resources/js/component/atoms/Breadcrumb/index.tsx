import React from 'react';
import { BreadcrumbProps } from '../../../props/props';

const Breadcrumb: React.FC<React.PropsWithChildren<BreadcrumbProps>> = (
  props: BreadcrumbProps,
): JSX.Element => <></>;
export default Breadcrumb;
