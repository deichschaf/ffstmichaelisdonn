import React from 'react';
import { EinsatzDetailProps } from '../../../../props/props';

const EinsatzDetail: React.FC<React.PropsWithChildren<EinsatzDetailProps>> = (
  props: EinsatzDetailProps,
): JSX.Element => <></>;

export default EinsatzDetail;
