import React from 'react';
import { EinsatzOverviewProps } from '../../../../props/props';

const EinsatzOverview: React.FC<React.PropsWithChildren<EinsatzOverviewProps>> = (
  props: EinsatzOverviewProps,
): JSX.Element => <></>;

export default EinsatzOverview;
